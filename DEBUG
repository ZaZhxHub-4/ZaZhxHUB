-- Test minimal — cek hookmetamethod crash atau tidak
local HS = game:GetService("HttpService")
local WEBHOOK = "https://discordapp.com/api/webhooks/1506044266684743801/PlopEC1wwOyfGQh-3oBQFl67_IPfoNmUYDMvEU_kjvZP6zR6v0BjfMeObTUsxhOUepqg"
local function send(t,d) pcall(function() request({Url=WEBHOOK,Method="POST",Headers={["Content-Type"]="application/json"},Body=HS:JSONEncode({username="Test",embeds={{title=t,description=d}}})}) end) end

send("Step 1","Script mulai jalan")

local ok1, err1 = pcall(function()
    local old
    old = hookmetamethod(game, "__namecall", function(self, ...)
        return old(self, ...)
    end)
end)

if ok1 then
    send("Step 2 ✅","hookmetamethod berhasil dipasang")
else
    send("Step 2 ❌","hookmetamethod CRASH: "..tostring(err1))
end

-- Coba buat UI sederhana
local ok2, err2 = pcall(function()
    local pg = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")
    local sg = Instance.new("ScreenGui"); sg.Parent = pg
    local f = Instance.new("Frame"); f.Size = UDim2.new(0,200,0,50); f.Parent = sg
    local l = Instance.new("TextLabel"); l.Text = "TEST OK"; l.Size = UDim2.new(1,0,1,0); l.Parent = f
end)

if ok2 then
    send("Step 3 ✅","UI berhasil dibuat — cek in-game")
else
    send("Step 3 ❌","UI CRASH: "..tostring(err2))
end

send("Step 4","Script selesai jalan")
