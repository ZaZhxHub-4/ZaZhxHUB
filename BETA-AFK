-- // PET FISHING BUG MONITOR: Hook semua remote terkait Fishing & Pets
-- // Tanpa mengirim data palsu, hanya memonitor untuk menangkap race condition
local webhook = "https://discordapp.com/api/webhooks/1450517804037046508/yi2ZuXEbsPhSQU6MaJ2fgmvm4YPGSz1G0q1oHpApUiYpRrDFCAd1-kuYC4q0sxZ8YOKY"
local HS = game:GetService("HttpService")
local RS = game:GetService("ReplicatedStorage")
local LP = game:GetService("Players").LocalPlayer

local function send(msg)
    pcall(function()
        local body = HS:JSONEncode({content = "`" .. os.date("%H:%M:%S") .. "` " .. msg})
        if http_request then http_request({Url=webhook, Method="POST", Headers={["Content-Type"]="application/json"}, Body=body})
        elseif syn and syn.request then syn.request({Url=webhook, Method="POST", Headers={["Content-Type"]="application/json"}, Body=body})
        else request({Url=webhook, Method="POST", Headers={["Content-Type"]="application/json"}, Body=body}) end
    end)
end

send("🔍 Memasang hook pemantau pada remote Fishing & Pets (mode aman)...")

local netFolder = RS.Packages._Index["sleitnick_net@0.2.0"].net

-- Daftar remote yang ingin kita monitor
local targetNames = {
    "RE/FishCaught",
    "RE/CatchFishCompleted",
    "RE/PlayFishingEffect",
    "RE/BaitCastVisual",
    "RE/BaitDestroyed",
    "RE/BaitSpawned",
    "RE/ObtainedNewFishNotification",
    "RE/FishingStopped",
    "RE/FishingMinigameChanged",
    "RE/CaughtFishVisual",
    "RF/UpdateAutoFishingState",
    "RF/MarkAutoFishingUsed",
    "RF/CancelFishingInputs",
    "RF/ChargeFishingRod",
    "RF/RequestFishingMinigameStarted",
    "RF/UpdateFishingRadar",
    "RF/Pets/Equip",
    "RF/Pets/Unequip",
}

local hooked = 0
for _, name in ipairs(targetNames) do
    local remote = netFolder:FindFirstChild(name)
    if remote then
        if remote:IsA("RemoteEvent") then
            pcall(function()
                local oldFire = remote.FireServer
                remote.FireServer = function(self, ...)
                    local args = {...}
                    local argsStr = ""
                    for i, v in ipairs(args) do
                        argsStr = argsStr .. tostring(v) .. (i == #args and "" or ", ")
                    end
                    send("📤 " .. name .. " : FireServer(" .. argsStr .. ")")
                    return oldFire(self, ...)
                end
                hooked = hooked + 1
            end)
        elseif remote:IsA("RemoteFunction") then
            pcall(function()
                local oldInvoke = remote.InvokeServer
                remote.InvokeServer = function(self, ...)
                    local args = {...}
                    local argsStr = ""
                    for i, v in ipairs(args) do
                        argsStr = argsStr .. tostring(v) .. (i == #args and "" or ", ")
                    end
                    send("📤 " .. name .. " : InvokeServer(" .. argsStr .. ")")
                    return oldInvoke(self, ...)
                end
                hooked = hooked + 1
            end)
        end
    end
end

send("✅ Terpasang " .. hooked .. " hook. Sekarang silakan mancing seperti biasa, tangkap ikan langka, dan perhatikan Discord saat bug pet terjadi.")
send("⚠️ Jangan gunakan auto-click atau exploit lain, biarkan bug terjadi alami.")
