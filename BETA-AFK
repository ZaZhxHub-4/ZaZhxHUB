-- // FINAL ATTEMPT: Race Condition Exploit via Toggle Auto-Fishing + Mark Auto
-- // Tidak ada hook, tidak ada monitoring tidak berguna.
-- // Hanya mengirim kombinasi remote yang mungkin mengacaukan timer pet.
-- // Lakukan saat kamu sudah berada di air, pet equip, dan auto-fishing ON (manual).

local RS = game:GetService("ReplicatedStorage")
local net = RS.Packages._Index["sleitnick_net@0.2.0"].net

local autoStateRF  = net:FindFirstChild("RF/UpdateAutoFishingState")
local markAutoRF   = net:FindFirstChild("RF/MarkAutoFishingUsed")
local cancelRF     = net:FindFirstChild("RF/CancelFishingInputs")
local minigameRF   = net:FindFirstChild("RF/RequestFishingMinigameStarted")

if not autoStateRF or not markAutoRF or not cancelRF then
    return print("Remote tidak lengkap.")
end

print("Race Condition Exploit aktif. Jangan matikan auto-fishing manual.")

task.spawn(function()
    while true do
        -- Spam toggle state dengan cepat (5x dalam 1 detik)
        for i = 1, 5 do
            pcall(function() autoStateRF:InvokeServer(true) end)
            task.wait(0.05)
            pcall(function() autoStateRF:InvokeServer(false) end)
            task.wait(0.05)
        end
        
        -- Panggil MarkAutoFishingUsed untuk mereset cooldown
        pcall(function() markAutoRF:InvokeServer() end)
        task.wait(0.1)

        -- Pastikan berakhir dalam keadaan ON
        pcall(function() autoStateRF:InvokeServer(true) end)

        task.wait(45) -- ulangi setiap 45 detik
    end
end)
