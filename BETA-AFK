-- // ZHX Fast Reel + Pet Bug Trigger v2.5
-- // UI Minimalis: Input delay, Toggle ON/OFF
-- // Remote hash method (proven working)

local RS = game:GetService("ReplicatedStorage")
local UIS = game:GetService("UserInputService")
local Players = game:GetService("Players")
local LP = Players.LocalPlayer

-- ========== REMOTE HASH FINDER ==========
local function findRemoteByLabel(label)
    local net = RS.Packages._Index["sleitnick_net@0.2.0"].net
    local children = net:GetChildren()
    for i, child in ipairs(children) do
        if child.Name == label then
            local inside = child:FindFirstChildOfClass("RemoteFunction") or child:FindFirstChildOfClass("RemoteEvent")
            if inside then
                local h = inside.Name:match("^R[FE]/([a-fA-F0-9]+)$")
                if h and #h == 64 then return inside end
            end
            local nxt = children[i+1]
            if nxt and (nxt:IsA("RemoteFunction") or nxt:IsA("RemoteEvent")) then
                local h = nxt.Name:match("^R[FE]/([a-fA-F0-9]+)$")
                if h and #h == 64 then return nxt end
            end
            break
        end
    end
    return nil
end

local cancelRF = findRemoteByLabel("RF/CancelFishingInputs")
local chargeRF = findRemoteByLabel("RF/ChargeFishingRod")
local minigameRF = findRemoteByLabel("RF/RequestFishingMinigameStarted")
local catchRE = findRemoteByLabel("RE/CatchFishCompleted") or findRemoteByLabel("RE/FishCaught")

if not cancelRF or not chargeRF or not minigameRF or not catchRE then
    error("Remote tidak ditemukan. Pastikan sudah di dalam game.")
end

-- ========== KONFIGURASI DEFAULT ==========
local config = {
    minigameDelay = 0.05,   -- jeda setelah minigame sebelum catch
    catchDelay = 0.01,      -- jeda antar catch spam
    catchCount = 3,         -- jumlah spam catch
    loopInterval = 0.3,     -- jeda antar siklus
    active = false
}

local fastReelThread = nil

-- ========== FUNGSI FAST REEL ==========
local function fastReelCycle()
    pcall(function() cancelRF:InvokeServer() end)
    task.wait(0.01)
    pcall(function() chargeRF:InvokeServer(1755848498.4834) end)
    task.wait(0.01)
    pcall(function() minigameRF:InvokeServer(1.2854545116425, 1) end)
    task.wait(config.minigameDelay)
    for i = 1, config.catchCount do
        pcall(function() catchRE:FireServer() end)
        task.wait(config.catchDelay)
    end
end

local function startFastReel()
    if config.active then return end
    config.active = true
    fastReelThread = task.spawn(function()
        while config.active do
            fastReelCycle()
            task.wait(config.loopInterval)
        end
    end)
end

local function stopFastReel()
    config.active = false
    if fastReelThread then
        task.cancel(fastReelThread)
        fastReelThread = nil
    end
end

-- ========== UI MINIMALIS ==========
local pg = LP:WaitForChild("PlayerGui")
local sg = Instance.new("ScreenGui")
sg.Name = "ZHX_FastReel"
sg.ResetOnSpawn = false
sg.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
sg.Parent = pg

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 220, 0, 180)
frame.Position = UDim2.new(1, -230, 1, -190)
frame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
frame.BackgroundTransparency = 0.1
frame.BorderSizePixel = 0
frame.ZIndex = 100
frame.Parent = sg
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)
Instance.new("UIStroke", frame).Color = Color3.fromRGB(72, 210, 130)

-- Title bar
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundTransparency = 1
title.Text = "⚡ Fast Reel v2.5"
title.TextColor3 = Color3.fromRGB(72, 210, 130)
title.TextSize = 14
title.Font = Enum.Font.GothamBold
title.ZIndex = 101
title.Parent = frame

-- Row 1: Minigame Delay
local function makeInput(y, labelText, default, callback)
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0, 100, 0, 20)
    lbl.Position = UDim2.new(0, 10, 0, y)
    lbl.BackgroundTransparency = 1
    lbl.Text = labelText
    lbl.TextColor3 = Color3.fromRGB(200, 200, 200)
    lbl.TextSize = 11
    lbl.Font = Enum.Font.Gotham
    lbl.ZIndex = 101
    lbl.Parent = frame

    local box = Instance.new("TextBox")
    box.Size = UDim2.new(0, 60, 0, 20)
    box.Position = UDim2.new(0, 110, 0, y)
    box.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    box.BorderSizePixel = 0
    box.Text = tostring(default)
    box.TextColor3 = Color3.fromRGB(255, 255, 255)
    box.TextSize = 11
    box.Font = Enum.Font.Gotham
    box.ZIndex = 101
    box.Parent = frame
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 4)

    box.FocusLost:Connect(function()
        local num = tonumber(box.Text)
        if num then callback(num) end
    end)
    return box
end

local minigameBox = makeInput(35, "Minigame Delay", config.minigameDelay, function(v) config.minigameDelay = v end)
local catchBox    = makeInput(60, "Catch Delay", config.catchDelay, function(v) config.catchDelay = v end)
local countBox    = makeInput(85, "Catch Count", config.catchCount, function(v) config.catchCount = math.floor(v) end)
local loopBox     = makeInput(110, "Loop Interval", config.loopInterval, function(v) config.loopInterval = v end)

-- Toggle button
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(1, -20, 0, 35)
toggleBtn.Position = UDim2.new(0, 10, 0, 140)
toggleBtn.BackgroundColor3 = Color3.fromRGB(20, 60, 30)
toggleBtn.BorderSizePixel = 0
toggleBtn.Text = "▶ START"
toggleBtn.TextColor3 = Color3.fromRGB(72, 210, 130)
toggleBtn.TextSize = 13
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.ZIndex = 101
toggleBtn.AutoButtonColor = false
toggleBtn.Parent = frame
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0, 6)

toggleBtn.MouseButton1Click:Connect(function()
    if config.active then
        stopFastReel()
        toggleBtn.Text = "▶ START"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(20, 60, 30)
    else
        startFastReel()
        toggleBtn.Text = "⏹ STOP"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(60, 20, 20)
    end
end)

-- Drag functionality
local dragging, dragStart, startPos = false, nil, nil
frame.InputBegan:Connect(function(inp)
    if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = inp.Position
        startPos = frame.Position
    end
end)
UIS.InputChanged:Connect(function(inp)
    if dragging and (inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch) then
        local delta = inp.Position - dragStart
        frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UIS.InputEnded:Connect(function(inp)
    if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

print("ZHX Fast Reel v2.5 loaded. Panel di kanan bawah.")
